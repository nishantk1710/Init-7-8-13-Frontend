# WS2 field-source gap report

Generated by `npm run gap-report`. Every field of every initiative view model,
and where its data comes from.

| Source | Meaning | Fields |
|---|---|---|
| `sap` | A real, exposed SAP property | 17 |
| `platform` | Data this system owns | 25 |
| `derived` | Computed from other mapped fields | 10 |
| `blocked` | Real SAP field, but no usable data yet (§1.3 / §1.7) | 8 |
| `gap` | **Nothing upstream produces this** | 14 |
| | **Total** | **74** |

## I07 — Recommendation

| Field | Source | Detail |
|---|---|---|
| `id` | `platform` | inventory_recommendations:recommendation_id |
| `material` | `sap` | MaterialSet.Matnr — description joined from MaterialDescriptionSet.Maktx |
| `plantId` | `sap` | MaterialPlantSet.Werks |
| `criticality` | `platform` | inventory_recommendations:criticality |
| `demandPattern` | `platform` | inventory_recommendations:demand_pattern |
| `status` | `platform` | inventory_recommendations:status |
| `leadTimeDays` | `sap` | MaterialPlantSet.Plifz — W2.9's lead-time source; platform column carries the same value today |
| `current` | `sap` | MaterialPlantSet.Minbe/Eisbe/Mabst — reorder point, safety stock and max stock as currently maintained in SAP |
| `recommended` | `platform` | inventory_recommendations:recommended_rop/recommended_safety_stock/recommended_max_stock |
| `annualConsumption` | `platform` | inventory_recommendations:consumptions_12m |
| `generatedAt` | `platform` | inventory_recommendations:created_on |
| `workflow` | `platform` | approvals:approver_role/decision/decision_date — one step per approval row for this recommendation |
| `avgDailyConsumption` | `derived` | annualConsumption / 365 |
| `risk` | `derived` | from criticality plus the size of the change between current and recommended |
| `workingCapitalImpact` | `blocked` | MaterialValuationSet.Verpr — needs unit price to compute; MaterialValuationSet returns zero rows live (§1.3) |
| `unitPrice` | `blocked` | MaterialValuationSet.Verpr — moving average price. Zero rows live (§1.3), so this is 0 until SAP answers why |
| `consumptionHistory` | `blocked` | GoodsMovementItemSet — derivable from consumption movements, but needs the movement-type set confirmed (still a pending business constant) |
| `circuit` | **gap** | Plant circuit (Crushing/Milling/Pumping/...). No SAP field carries it and no platform file produces it. Either it comes from a plant-master mapping VZI would have to supply, or the UI should stop showing it. |
| `factors` | **gap** | Human-readable 'why this recommendation' bullets. inventory_recommendations.reason holds ONE sentence, not the structured label/detail pairs the UI renders. Whatever generates recommendations must emit these. |
| `championChallenger` | **gap** | Champion/challenger model names, descriptions and accuracy percentages. Pure ML metadata — it can only come from the model-serving layer, which does not exist yet. |
| `oarColdStart` | **gap** | Similar-material suggestions and a confidence band for OAR materials with no history. An AI output, not a SAP read. |
| `serviceLevelTarget` | **gap** | Target service level per material. Not maintained in SAP and not in any platform file — needs a policy decision from VZI, likely by criticality band. |
| `leadTimeVarianceDays` | **gap** | Lead-time variability. MARC.PLIFZ is a single planned figure with no variance. Would have to be computed from PO history (EKET/EKBE) over time. |
| `scenarioNote` | **gap** | Demo-only annotation on the hand-written fixtures. Should not survive into real data. |

## I08 — RepairChain

| Field | Source | Detail |
|---|---|---|
| `id` | `platform` | repair_cases:case_id |
| `material` | `sap` | MaterialSet.Matnr — description joined from MaterialDescriptionSet.Maktx |
| `plant` | `sap` | MaterialPlantSet.Werks — the CODE is real; the site NAME is not - see reference-data.ts |
| `repairPR` | `platform` | repair_cases:repair_pr |
| `repairPO` | `platform` | repair_cases:repair_po/repair_po_item |
| `repairStatus` | `platform` | repair_cases:stage |
| `raisedAt` | `platform` | repair_cases:removed_on |
| `expectedReturn` | `platform` | repair_cases:expected_back_on |
| `receivedAt` | `platform` | repair_cases:returned_on |
| `notes` | `platform` | repair_cases:notes |
| `declarationStatus` | `platform` | repair_attestations:attestation_id — present and complete -> Completed; missing -> Required |
| `reorderPoint` | `sap` | MaterialPlantSet.Minbe |
| `stockOnHand` | `sap` | StorageLocationStockSet.Labst — unrestricted stock summed across storage locations for the plant |
| `vendor` | `sap` | PurchaseOrderSet.Lifnr — vendor on the repair PO, joined to VendorSet.Name1 for a display name |
| `newUnitLeadTimeDays` | `sap` | MaterialPlantSet.Plifz |
| `daysOpen` | `derived` | days between raisedAt and today |
| `daysRemainingInRepair` | `derived` | days between today and expectedReturn; negative once overdue |
| `agingBucket` | `derived` | daysOpen bucketed into 0-15 / 16-30 / 31-45 / 46-60 / 60+ |
| `qtyUnderRepair` | `derived` | repair PO quantity, zero once the case reaches BACK_IN_STOCK |
| `receiptStatus` | `derived` | from the repair case stage plus returned_on |
| `repairCost` | `blocked` | PurchaseOrderItemSet.Netwr — the repair PO's net value. Live today, but Netwr is Edm.String and must be parsed, never coerced by shape |
| `newUnitCost` | `blocked` | MaterialValuationSet.Stprs — standard price of a new unit; MaterialValuationSet returns zero rows live (§1.3) |
| `poIssuedAt` | `blocked` | PurchaseOrderSet.Aedat — PO creation date; joinable once repair POs are read live rather than from the fixture |
| `sentToVendorAt` | **gap** | Date the unit physically left for the vendor. No SAP field and no platform column carries it — it would come from a goods-issue movement whose movement type is still an unconfirmed business constant (D7). |

## I13 — UtilizationLedgerLine

| Field | Source | Detail |
|---|---|---|
| `id` | `platform` | consumption_plans:plan_id |
| `trackingId` | `platform` | consumption_plans:session_id |
| `reservation` | `sap` | ReservationItemSet.Rsnum/Rspos — real property names, confirmed, and live since the 09-Sep 2026 sweep fixed §1.3's zero-row blocker (now 1,000 rows) — falls back to the plan's own numbers when a plan's reservation is not (yet) wired in |
| `material` | `sap` | MaterialSet.Matnr — description joined from MaterialDescriptionSet.Maktx |
| `plant` | `sap` | MaterialPlantSet.Werks — the CODE is real; the site NAME is not - no exposed SAP field maps Werks to a mine site, and SAP has 5 plant codes against the app's 3 named plants. See reference-data.ts |
| `qtyRequested` | `sap` | ReservationItemSet.Bdmng — requirement quantity. Live since 09-Sep 2026 (§1.3); consumption_plans.planned_quantity stands in when no reservation is joined |
| `qtyIssued` | `sap` | ReservationItemSet.Enmng — quantity withdrawn. Live since 09-Sep 2026 (§1.3); 0 when no reservation is joined |
| `plannedConsumptionDate` | `sap` | ReservationItemSet.Bdter — requirement date. Live since 09-Sep 2026 (§1.3); consumption_plans.planned_use_date stands in when no reservation is joined |
| `uom` | `sap` | ReservationItemSet.Meins — live since 09-Sep 2026 (§1.3); defaults to EA when no reservation is joined |
| `qtyConfirmedUsed` | `platform` | utilisation_status:confirmed_used |
| `requester` | `platform` | consumption_plans:requester — the requester ID is real; mapping it to a named user is not - platform data carries VZIREQ01..05 with no link to the app's user catalogue. See reference-data.ts |
| `purpose` | `platform` | consumption_plans:purpose |
| `stage` | `derived` | from the utilisation status plus which SAP documents exist in the chain |
| `exception` | `platform` | exceptions:exception_type — Consumption Overdue / No Longer Required |
| `replanReason` | `platform` | utilisation_status:reason |
| `replanNewDate` | `platform` | utilisation_status:replanned_date |
| `agingDays` | `derived` | days between the planned consumption date and today |
| `documentChain` | `derived` | assembled from the reservation, its PR (Banfn/Bnfpo), the PO, and goods movements |
| `qtyReceived` | `blocked` | GoodsMovementItemSet — goods-receipt quantity against the PR/PO. Needs the movement-type set confirmed — still a pending business constant |
| `unitPrice` | `blocked` | MaterialValuationSet.Verpr — zero rows live (§1.3) |
| `department` | **gap** | Requester's department. Not on the reservation and not in any platform file. Would come from an HR/user directory, which this system does not have. |
| `project` | **gap** | Project reference. RESB has no project field in the exposed projection. If VZI wants it, it is an exposure request. |
| `jobWorkOrder` | **gap** | Work-order reference. ReservationItemSet.Aufnr (order number) IS exposed and is the obvious candidate — but confirm with VZI that Aufnr is what this field means before wiring it. |
| `equipment` | **gap** | Equipment reference. No exposed source; would need EQUI/technical-object data that is not in either service. |
| `allocationMethod` | **gap** | How a consolidated PR/PO splits across requesters (Scenario H). A platform concept with no producer yet. |
| `consolidatedGroupId` | **gap** | Groups ledger lines rolled into a shared PR/PO. Same as above — a platform concept nothing currently emits. |

## The gaps, in one place

These are the fields the UI renders today that nothing upstream can produce.
Each is a decision: either something starts producing it, or the UI stops showing it.

- **I07 `circuit`** — Plant circuit (Crushing/Milling/Pumping/...). No SAP field carries it and no platform file produces it. Either it comes from a plant-master mapping VZI would have to supply, or the UI should stop showing it.
- **I07 `factors`** — Human-readable 'why this recommendation' bullets. inventory_recommendations.reason holds ONE sentence, not the structured label/detail pairs the UI renders. Whatever generates recommendations must emit these.
- **I07 `championChallenger`** — Champion/challenger model names, descriptions and accuracy percentages. Pure ML metadata — it can only come from the model-serving layer, which does not exist yet.
- **I07 `oarColdStart`** — Similar-material suggestions and a confidence band for OAR materials with no history. An AI output, not a SAP read.
- **I07 `serviceLevelTarget`** — Target service level per material. Not maintained in SAP and not in any platform file — needs a policy decision from VZI, likely by criticality band.
- **I07 `leadTimeVarianceDays`** — Lead-time variability. MARC.PLIFZ is a single planned figure with no variance. Would have to be computed from PO history (EKET/EKBE) over time.
- **I07 `scenarioNote`** — Demo-only annotation on the hand-written fixtures. Should not survive into real data.
- **I08 `sentToVendorAt`** — Date the unit physically left for the vendor. No SAP field and no platform column carries it — it would come from a goods-issue movement whose movement type is still an unconfirmed business constant (D7).
- **I13 `department`** — Requester's department. Not on the reservation and not in any platform file. Would come from an HR/user directory, which this system does not have.
- **I13 `project`** — Project reference. RESB has no project field in the exposed projection. If VZI wants it, it is an exposure request.
- **I13 `jobWorkOrder`** — Work-order reference. ReservationItemSet.Aufnr (order number) IS exposed and is the obvious candidate — but confirm with VZI that Aufnr is what this field means before wiring it.
- **I13 `equipment`** — Equipment reference. No exposed source; would need EQUI/technical-object data that is not in either service.
- **I13 `allocationMethod`** — How a consolidated PR/PO splits across requesters (Scenario H). A platform concept with no producer yet.
- **I13 `consolidatedGroupId`** — Groups ledger lines rolled into a shared PR/PO. Same as above — a platform concept nothing currently emits.

## Blocked on SAP, not missing

These map to real SAP fields that exist but return nothing usable today.

- **I07 `workingCapitalImpact`** — MaterialValuationSet.Verpr — needs unit price to compute; MaterialValuationSet returns zero rows live (§1.3)
- **I07 `unitPrice`** — MaterialValuationSet.Verpr — moving average price. Zero rows live (§1.3), so this is 0 until SAP answers why
- **I07 `consumptionHistory`** — GoodsMovementItemSet — derivable from consumption movements, but needs the movement-type set confirmed (still a pending business constant)
- **I08 `repairCost`** — PurchaseOrderItemSet.Netwr — the repair PO's net value. Live today, but Netwr is Edm.String and must be parsed, never coerced by shape
- **I08 `newUnitCost`** — MaterialValuationSet.Stprs — standard price of a new unit; MaterialValuationSet returns zero rows live (§1.3)
- **I08 `poIssuedAt`** — PurchaseOrderSet.Aedat — PO creation date; joinable once repair POs are read live rather than from the fixture
- **I13 `qtyReceived`** — GoodsMovementItemSet — goods-receipt quantity against the PR/PO. Needs the movement-type set confirmed — still a pending business constant
- **I13 `unitPrice`** — MaterialValuationSet.Verpr — zero rows live (§1.3)
